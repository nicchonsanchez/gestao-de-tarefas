# Template de variáveis de ambiente da RAIZ do monorepo.
# Copie para .env.local (ignorado pelo git) e ajuste conforme necessário.
# Valores específicos por app ficam em apps/api/.env e apps/web/.env.local

# ----------- Docker Compose (infra local) -----------
# Opcional — o docker-compose.yml usa defaults consistentes.
POSTGRES_USER=ktask
POSTGRES_PASSWORD=ktask
POSTGRES_DB=ktask
